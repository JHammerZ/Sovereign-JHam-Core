# Sovereign-JHam-Core/zero_code_manifest.py
from blueprint_bypass import BlueprintBypass
from universal_adapter import UniversalAdapter

def instant_manifest(pilot_intent):
    """
    The final bridge: Words -> Universal 3D Manifest. 
    No manual coding required in the target engine.
    """
    # 1. Pilot speaks (Thought-Form)
    # 2. Bypass injects DNA directly to the Bus
    bypass = BlueprintBypass()
    dna = bypass.inject_asset(pilot_intent)
    
    # 3. Adapter broadcasts to UE5/Apex/Holographic
    adapter = UniversalAdapter()
    adapter.broadcast_to_external(dna)
    
    print(f"[SUCCESS]: '{pilot_intent}' is now a physical 3D constant.")
